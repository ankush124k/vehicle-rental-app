import React from 'react';
import { Typography } from '@mui/material';

const Step1Name = () => {
  return (
    <div>
      <Typography variant="h6">Step 1: What is your name?</Typography>
      {/* We will add form fields and buttons here in the next step */}
    </div>
  );
};

export default Step1Name;
